[pytest]
addopts = -ra --strict-config --strict-markers
testpaths = tests
python_files = test_*.py
markers =
    slow: marks tests as slow; exclude in default runs.
    gui: marks GUI-dependent tests that may require a display.
filterwarnings =
    ignore::DeprecationWarning
